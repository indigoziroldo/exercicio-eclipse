package fundamentos.operadores;

public class Operadores {

	public static void main(String[] args) {
		// OPERADORES - número de operandos em cima de um determinado operador
		
	/* Binário = soma, temos um operador (+) e dois operandos (valores na soma)
	 * 
	 * Unário = a++, temos um operador ++, e um operando, que irá adicionar um acréscimo ao valor
	 * 
	 * Ternário = v ? v : v, temos 2 operadores ? e :, e 3 operandos
	 * 
	 * Operador antes do operando PREFIX  ++A
	 * Operador depois do operando POSTFIX  A++
	 * Operador entre os operandos INFIX A+B
	 * 
	 * E = &&
	 * OU = ||
	 * OU Exclusivo, um ou o outro, ^
	 * 
	 * notV = 
	 * notF = 
	 * 
	 * 
	 * 
	 * 
	 * 
	 */
	}
	
}
